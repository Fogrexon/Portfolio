import React from 'react';
import { BigTitle, Galleries } from './Components';
import '../App.scss';

export default () => (
  <React.Fragment>
    <BigTitle />
    <Galleries />
  </React.Fragment>
);