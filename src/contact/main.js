import React from 'react';
import { BigTitle } from '../utils/Components';

export default () => (
  <React.Fragment>
      <BigTitle title='Contact' >
        <p>
          連絡先です
        </p>
      </BigTitle>
  </React.Fragment>
);
